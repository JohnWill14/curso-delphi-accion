program Project1;

uses
  Vcl.Forms,
  principal in 'principal.pas' {Form3},
  sobreUnit in 'sobreUnit.pas' {Form4};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm3, Form3);
  Application.CreateForm(TForm4, Form4);
  Application.Run;
end.
